default:
  paths:
    features: acceptance
    bootstrap: %behat.paths.features%/bootstrap
  context:
    class: Officine\Amaka\Specs\EndToEndSpecContext
    parameters:
      amaka_command: %behat.paths.base%/bin/amaka
      context_directory: %behat.paths.bootstrap%

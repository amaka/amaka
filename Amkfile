<?php

return array(
    $this->task('test'),
    $this->task(':spec', function() { system("vendor/bin/behat"); }),
    $this->task(':build')->dependsOn('test', ':spec'),
    $this->task(':dist')->dependsOn(':build'),
);
